# ==============================================================================
# vision skill 依赖 — 一键安装: pip install -r requirements.txt
# 详细说明: .claude/skills/vision/auto_config/requirements.txt
# ==============================================================================

# ----- 多模态 API 厂商 (按需：只用哪个厂商就装哪个，不必全装) -----
zai              # 智谱 GLM (glm-4.6v)
openai           # OpenAI (gpt-4o)
anthropic        # Anthropic (claude-sonnet-4-6)
google-genai     # Google (gemini-2.5-flash)

# ----- 远程 URL 下载 + 图像处理 -----
requests
Pillow

# ----- OCR 文字提取 (无 API key 时兜底) -----
easyocr          # 离线 OCR，中英文，首次运行自动下载模型 ~200MB
pytesseract      # 可选：需系统安装 tesseract（brew install tesseract）
